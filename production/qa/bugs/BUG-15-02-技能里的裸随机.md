# BUG-15-02 · 技能里 13 处裸 `Math.random`

- 严重度：P2（守卫红；行为本身不错）
- 来源：15 的守卫 4（决策 22：随机只许在动画级抖动）
- 复现：`grep -rn "Math.random" Frontend-3D/src/Game/State/skills` → build / needs / routine / wander 共 13 处
- 期望：技能里没有裸随机；时长抖动和掷点走一个有名字的口，让"这是动画级的"一眼看得出
- 实际：秒数抖动（idleAfter、睡多久、站多久）和两处掷点（打不打盹、走不走向玩家）直接写 Math.random
- 根因：01 拆技能时把原来身体里的抖动原样搬过去，没立规矩
- 修法：`State/skills/jitter.ts`（`jitterSeconds` / `chance`）是唯一允许的地方，13 处换成它；守卫用例 `hardcodeGuards.test` 只放行这一个文件。行为不变（还是随机，只影响多久 / 要不要，秒数进 Intent 再上网线，两端不分叉）
- 同类：`Systems/residents` 一处都没有（内容抽签早就是 hashSeed）

# 13 · 个人剧情线

> 状态：**13 已完成（薇尔第三幕留白；软凳、风铃配方待家具）** · 2026-09-06 · 本提交 + 存档 v46 · 前置：05 委托、09 新邻居、10 信箱
>
> 一句话：每位居民一条三幕的线，节拍由好感档和委托驱动，内容零代码；
> 三条线各接一个主线节点——小镇、餐厅、魔女——让主线是"因为邻居"而不是"系统解锁"。

## 玩家看到什么

- **咕噜（史莱姆）·「怕黑」**
  第一幕：搬来第三天，他请你带他去井边（`visit_me` 变体：跟着你走）。第二幕：委托"想要一盏灯"，完成当晚他家窗亮成暖色、门口摆着灯，信箱收到一封歪歪扭扭的信。第三幕（家人档）：他送你"史莱姆做的软凳"（专属家具），并且说出他为什么怕黑——一段长对话，之后闲聊池多一类"关于老家"的段。**接主线**：软凳是第一件"邻居做的家具"，餐厅（期 8）开张时他第一个来吃（决策 23 的客源）。
- **阿茜（狐狸）·「河对岸」**
  第一幕：她总念叨小镇，想开一条近路。第二幕：委托"把这个送到小镇的杂货铺"——**你第一次因此过桥**，到小镇后触发解锁（**接主线决策 22：小镇解锁由这条委托触发**，不再是系统自动开）。第三幕：她从小镇带回一封不是她写的信（`story` 类信件，寄件人空白）——这是魔女线的第一个钩子。
- **薇尔（精灵）·「会发光的土地」**
  第一幕：她说被照料的土地会发光，请你在她家旁边种点什么（农田系统的委托变体 `plant`）。第二幕：`visit_me` 进屋，她教你风铃的事，解锁她的专属家具配方。第三幕：等魔女主线定稿——她是"像人"的第一个角色，文明设定在这里立。**这一幕留空，不写占位内容。**

## 动森原型与取舍

动森没有个人剧情线，只有友好度台阶上的固定事件（送照片、改口头禅）。个人线是我们加的，取的是**动森的节拍感**：不靠任务面板推，靠"今天他主动说了一句不一样的话"。所以三条线的每个节拍都是**居民主动发起**（`favor` 那套走过来），不是任务板上的一行。

## 数据

全部是现有引擎的数据：`storyRules` + `favorDefinitions` + `letterDefinitions` + `dialogues` + `events`。

### 事件（Core/src/Data/events/index.ts）

每位一个 `EventDefinition`，阶段就是幕：

```ts
{ id: "arc_slime", stages: ["settled", "afraid_of_dark", "lamp_lit", "opened_up", "done"] },
{ id: "arc_fox",   stages: ["settled", "wants_shortcut", "delivered_to_town", "brought_letter", "done"] },
{ id: "arc_spirit",stages: ["settled", "asked_to_plant", "taught_chimes", /* 第三幕待定 */] },
```

### 规则（每位 6~10 条，形状统一）

| 触发 | 条件 | 效果 |
|---|---|---|
| `resident_moved_in` subject=slime | — | `set_event_stage(arc_slime, settled)` |
| `day_started` | `event_stage(arc_slime, settled)` + `days_since_moved_in ≥ 3` | `offer_favor(slime_walk_to_well)`（**新效果**：直接提出一件委托，绕过日抽签） |
| `favor_completed` subject=slime_walk_to_well | — | `set_event_stage(afraid_of_dark)` + `add_memory` |
| `affection_reached` subject=slime:familiar | `event_stage(afraid_of_dark)` | `offer_favor(slime_wants_lamp)` |
| `favor_completed` subject=slime_wants_lamp | — | `set_event_stage(lamp_lit)` + `send_letter(slime_thanks_lamp)` + `porch_place(cloud_lamp)` |
| `affection_reached` subject=slime:family | `event_stage(lamp_lit)` | `start_dialogue(slime_opens_up)` + `present_items(signature)` + `set_event_stage(opened_up)` |
| `dialogue_event` subject=slime_arc_done | — | `set_event_stage(done, complete)` |

阿茜第二幕的关键规则：`favor_completed` subject=fox_deliver_town → `unlock_feature("town_travel")`。**现有的小镇解锁规则改成由它触发**（今天是怎么解锁的要查一下再改，标为执行时确认项）。

### 新效果 / 条件

- 效果 `offer_favor: { favorId }`（05 的委托系统提供入口）。
- 委托 kind `escort`（"跟着我走到某处"）：`walk_to` 目标是"玩家身边 1.5 m"的持续跟随 Intent，到达场所结束。给咕噜第一幕用；也给以后"带访客参观"用。
- 委托 kind `plant`：`wants` 换成 `plantedNear: { buildingId, radius, cropId? }`，读农田系统"该建筑附近有没有作物"。

### 闲聊池的"幕后段"

每幕完成后解锁一类闲聊（条件 `event_stage`），每幕 3 段 × 3 幕 × 3 人 = 27 段，让"经历过的事"在日常里留下痕迹。这是 03 的内容量之外的。

## 接主线的三个口

| 口 | 现在 | 改成 |
|---|---|---|
| 小镇解锁（决策 22） | 待查（可能是功能默认开 / 剧情某条） | 阿茜第二幕委托完成 → `unlock_feature(town_travel)` |
| 餐厅客源（决策 23） | 客人默认先是居民 | 咕噜第三幕完成 → 餐厅第一天的报纸头条"咕噜是第一位客人"（`dayRecord` 事实由规则写） |
| 魔女线 | 未定稿 | 阿茜第三幕带回的信是 `letterDefinitions` 里一条 `story` 信，正文留 `letter.witch.first`；魔女线定稿后只改文案和后续规则 |

## 联机

个人线全部是房主的剧情，做客时剧情系统闭嘴（既有）。房客看到的副作用（门口的灯、门牌、居民位置）走各自切片。**无新同步需求。** 联机验收只补一条：房主推进咕噜第二幕 → 房客那边门口出现灯、窗亮暖色。

## 指令

| 指令 | 干什么 |
|---|---|
| `/npc <谁> arc` | 打印他的线在哪一幕、下一幕等什么条件 |
| `/npc <谁> arc next` | 强制推进一幕（点火对应规则） |
| `/rule <id>` | 既有 |

## 存档

`events` 里三条新事件（现有 `progression.events` 存档结构，无新字段）。协议不变。

## 刻意不做的

- 任务面板 / 追踪标记；失败分支；限时；薇尔第三幕占位内容；居民之间的联动剧情（06 的八卦已经在做"软联动"）。

## 最容易写错的三处

1. **在代码里判"第几幕"。** 全在规则条件里。
2. **小镇解锁改挂委托后，老档已经解锁的玩家**：规则条件加 `feature_unlocked(town_travel) == false`，避免重复触发；已解锁的老档阿茜第二幕直接跳到 `delivered_to_town`（迁移里按 `unlockedFeatureIds` 补阶段）。
3. **`escort` 跟随把玩家挡住。** 跟随目标点在玩家**身后** 1.5 m，让路逻辑照常。

## 验收

- Core `storyAudit`：三条事件的每个阶段至少有一条规则能到达、至少有一条闲聊段引用；`offer_favor` 引用的 favorId 存在。
- Frontend `arcs.test`：三条线从 `settled` 到最后一幕逐条规则点火，每步断言效果（委托出现 / 信到 / 门口灯 / 功能解锁）；老档已解锁小镇 → 阿茜阶段补齐。
- 浏览器：`/npc slime arc next` 三次，每幕截图（井边跟随、灯亮暖色、软凳面板）；阿茜第二幕真的走一趟小镇。
- 联机双端：见上。

## 自查（2026-09-05）

| 抓到的 | 改法 |
|---|---|
| 想给个人线做一个"章节"数据结构 | 现有 `events.stages` 就是章节，不另造 |
| 剧情直接提出委托没有口 | 加 `offer_favor` 效果 |
| 薇尔第三幕想先写占位 | 不写：占位内容会被当成定稿 |
| 小镇解锁改挂委托会撞老档 | 迁移补阶段 + 条件防重复 |

## 收工记录（2026-09-06）

**做成的样子**：三条线 = `Data/events` 的 `arc_slime / arc_fox / arc_spirit`（阶段就是幕）+ `Data/story` 里 `arc_*` 十七条规则 +
`Data/residents/arcs.ts` 的目录（每幕由哪条规则立、等什么，给 `/npc <谁> arc` 和审计用）。每幕等的是委托做成 / 好感到档 /
他回来——没有一幕是日期到了就演的；**每条立幕的规则都卡在前一幕上**（`requiresEventStage`），好感先到档的等下一幕的规则挂在
day_started 上次日补。新效果 `offer_favor`（绕过抽签直接提）；委托加两种 kind：`escort`（接了他跟着你走，你到那种场所就算——
新技能 escort，优先级 55，一步一步跟在你身边 1.6 米）和 `plant`（她家 6 米内有播了种的田，按 F 交付）；deliver 加 `toMap`
（踏上那张图就算送到，没有收件人）。**小镇从此由阿茜第二幕的委托解锁**：桥头出入口挂 `requiresFeature: town_travel`，锁着踩上去
冒一句、六秒一次；老档 v46 迁移补上 `town_travel`，三位已搬来的补阶段（咕噜灯做完过的补到 lamp_lit、阿茜补到 delivered_to_town）。
幕后段 24 段闲聊挂在 `event_stage` 上（阶段一变换下一批；阿茜送到那天那段权重 100 一天一次）；咕噜家人档次日早上主动来说
`slime_opens_up`（五句，最后一句发 `slime_arc_done`）；灯亮那晚一封歪歪扭扭的信；阿茜下次从镇上回来带回 `witch_from_town`
（寄件人空白，拆开 = 她那条线走完）。指令 `/npc <谁> arc [next]`。

**和文档不一样的地方**

| 文档写的 | 实际 | 为什么 |
|---|---|---|
| 阿茜第三幕的信正文留 `letter.witch.first` | 另一封 `witch_from_town`（懒散口吻，没署名） | `witch_first` 是 14 开场那三行条子，不能一封信两用 |
| 咕噜第三幕送"软凳"（专属家具） | 专属家具还是 04 那条规则（家人档当天送云朵灯顶着）；`slime_opens_up` 只说话，不再送第二件 | 软凳要建模，先问参考图；两件专属家具会让 04 的 `signature_*` 规则和这条线打架 |
| "窗亮成暖色" | 没做：02 的窗灯本来就是暖色（有人在家夜里亮）；立了旗子 `slime_lamp_lit` 留给表现层 | 现有窗灯已经是那个效果；再加一档要改布景层的材质分档，等 15 看要不要 |
| escort 跟随点在玩家身后 1.5 m | 玩家身边 1.6 m（`findSpotNear`），不是身后 | 技能上下文里没有玩家朝向，"身后"算不出来；让路逻辑照常兜底 |
| `plant` 用农田系统的"该建筑附近有没有作物" | 看 `farm_plot` 的 `state.seedItemId`（播了种就算，不等长出来） | 农田没有"作物实体"，播种就是田的状态 |
| 薇尔第二幕"解锁专属家具配方" | `unlock_feature("recipe_wind_chime")`，配方那头暂时没有风铃可挂 | 风铃家具没有；旗子先立着，家具来了挂上去 |
| 条件 `feature_unlocked(town_travel) == false` 防重复 | 没加：立幕规则卡在前一幕上，`unlock_feature` 本身幂等 | 少一个条件，老档（迁移后 town_travel 已开）阿茜的线也不会卡住 |
| 餐厅客源（决策 23） | 记一笔报纸事实 `resident_arc_done` + 旗子 `slime_arc_done` | 餐厅那期还没有，留旗子给它读 |
| 咕噜走开时说"我也想做点什么"、两位 friends 各拿一本书 | 没做 | 前者是一句台词，等 15 的文案轮；后者 06 没有"一起坐"的动作 |

**踩的坑**：调试口硬提的委托能把线倒着推——浏览器里咕噜在 lamp_lit 阶段被我 `/npc favor offer` 陪走了一趟井边，
`favor_completed` 把阶段退回 afraid_of_dark。立幕规则全部加了 `requiresEventStage`（前一幕），用例守着。迁移第一版直接读
`save.ownWorld.progression`，用例里有只带一半形状的档 → 判空。`as const` 的委托表加了没 reward 的条目后，`favor.reward` 在联合
类型上不存在——规则那边按接口类型看表。

**没做**：薇尔第三幕（魔女主线未定稿，不写占位）；软凳 / 风铃家具（参考图）；联机双端（房主推进第二幕 → 房客门口出现灯，
走的是 08 的 interiors 切片，没实测）；`escort` 期间他挡路时的让路只靠 yielding 兜底，没有专门测。

## 验收结果

- Core 492 通过（3 个时区旧失败），新增 arcs 4 条（审计干净、目录 ↔ 事件 ↔ 规则、三种新口、幕序）；Frontend 788 通过（arcs 4 条：
  三条线逐幕、桥头锁着、escort 跟随、plant 看田、指令；migrations 2 条），`tsc -p tsconfig.app.json` / `typecheck:tests` 干净，
  改过的文件 lint 干净；Backend 114 通过。
- 浏览器（宿主档，v46 迁移后）：`/npc slime arc` = lamp_lit、`/npc fox arc` = delivered_to_town、`/npc spirit arc` = settled（迁移补的）；
  `/npc favor offer/accept slime_walk_to_well` → 咕噜起身跟着走（`/npc slime where`：Intent 来自 escort），玩家到井边 → 委托 done、
  他开口"到了！有你在，路好像一下子变短了"。`13-验收-陪到井边.jpg`。桥头锁着的提示在用例里验（真档小镇已开）。

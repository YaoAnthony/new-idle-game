# 石傀儡与建造流程

> 状态：**五期全部完成** · 见文末验收 · 2026-08-22 · 前置：期 0–2 + 女巫小屋三轮全部完成
>
> 标准同前四份：任何人不看聊天记录也能照着做完并验收。

## 一句话

领地上坐着一尊无头石傀儡。玩家在院子另一头捡到他的头，走过去按 F 装上，他苏醒；
和他说话打开建造面板；面板里买一张图纸；拿着图纸选址落下蓝图；石傀儡走过去把它建起来。
建好之后对着建筑按 F 能迁移 / 拆除 / 升级 / 看详情。

---

## 拍板过的（2026-08-22，用户定）

| # | 决策 | 依据 |
|---|---|---|
| G1 | 面板点击 → **扣材料、拿到一张图纸（物品）** → 玩家自己拿着图纸去选址 | 用户选。和「他一开始会给你图纸，你拿着图纸按 F 就会有蓝图」完全对上；图纸是真物品，以后能当任务奖励 / 商店商品 |
| G10 | **没有对话**：对着醒着的石傀儡按 F **直接开建造面板** | 用户定（"不用说话，点开就是面板"）。省掉一整套剧情数据——`storyRules` 和 `dialogueDefinitions` 都是空数组，本来要从零写。他是工头不是村民，走过去就该看到能盖什么 |
| G11 | 他有**简单的自主游荡**，守在自己那块地方（驻地半径 5 米） | 用户定（"先做一版，反正本来他也要寻路算法"）。守着一个位置本来也是"干活的"该有的样子 |
| G2 | 石傀儡在建 A 时下 B 的单 → **B 排队**：蓝图落地、围栏立起、进度条 0%，但傀儡不去 | 用户选。「B 不会动工」变成看得见的东西；以后多一尊傀儡就是多一个工位，规则不用改 |
| G3 | 建造面板**本期只吃金币，且 l1 储金罐要 0 枚** | 用户定（测试用）。也是 Core 早就写下的硬约束——`logic/goldJar.ts:31` 注释：第一只罐的建造代价必须为 0，否则玩家永远攒不出建罐的钱，死锁 |
| G4 | 材料是**数组**，不是单个数字 | 用户定。好消息：`upgradeCost` 本来就是 `Array<{itemId, quantity}>`，照抄它给"建造代价"就行 |

## 需要你点头的（我的判断 + 理由，不同意就说）

| # | 我打算这么做 | 理由 | 代价 |
|---|---|---|---|
| G5 | **石傀儡走 `PetAgent`，加一个 `role` 字段**，不另起 `GolemAgent` 类 | `PetAgent` 已经有：A* 带体型寻路、休眠/苏醒、碰撞体、存档、按 F 交互竞争、对话接线。全都是石傀儡要的。`petAgent.ts:39` 自己写着"实例化不继承，真出现『数字表达不了的独有行为』再考虑子类" | `role: "worker"` 的生物要跳过吃/喝/亲近三支行为；`PetTaste`、好感度这些字段对它无意义（留空） |
| G6 | 头是**摆在地上的家具**（`golem_head`），走近气泡「右键 拾取」 | 仓库已有的拾取约定（`ACTION_KEY.pickup = "右键"`）。做成掉落物的话半径 0.6 走过去自动吸走——剧情道具不该是路过顺手捡的 | 无 |
| G7 | 施工时长**每级一个数**（`buildDuration`，字段早就留好了），l1 储金罐给 **20 秒** | 测试期要短到能一眼看完进度条；`buildDuration` 是 `Record<levelId, number>`，以后调平衡只改数据 | 无 |
| G8 | 工地 = 一个**已经存在的 `BuildingPlacement`**，带 `construction`，模型换成"半成品 + 围栏" | 不另立"工地"实体：工地就是这栋建筑的一个阶段。占地校验、迁移、拆除全部立刻复用现成规则 | `construction` 要补一个 `startUtc`（现在只有 `finishUtc`，算不出百分比） |
| G9 | 储金罐**沿用现有三级模型**，不重做 | `Buildings/goldJar.ts` 里三级都有：石座 + 两段圆台罐腰 + 铜环 + 会涨的液面（`gold-surface` 节点），l2 加木架和引水铜管，l3 三罐品字加刻纹石台 | 你要是看了不满意，重做是独立一笔，不挡这条链 |

---

## 现状盘点：哪些是现成的，哪些是空的

### 现成能直接用（这条链最大的运气）

| 要的东西 | 现成的 |
|---|---|
| 两步确认选址 | `BuildingPlacementController`（311 行）：**真模型半透明虚影**（不是方块）+ 地面占地框 + 整格吸附（奇偶宽都对）+ R 转向 + 选定后虚影不消失可以走开绕圈看 + [确认][重选][取消]。今天唯一入口是 `/site` 指令 |
| 放置合法性 | `previewPlacement()` 同时喂预览和提交，绿色显示和提交结果不可能打架。四条理由齐全：领地外 / 压楼 / 压家具 / 超实例上限 |
| 储金罐模型 | 三级全有，液面按 `state.fill` 插值 |
| 生物寻路 | `PetAgent.seekNear(x, z, reach)`，A* 带 `clearanceRadius`，大家伙不钻缝 |
| 休眠 / 苏醒 | `PetAgent.state === "sleeping"`、`placeSleepingPet()`、`wakeUp()`、StoryEffect `pet_wake` |
| 带骨架的生物建模范本 | `recipes/shushu.ts`（476 行）：pivot 骨架 + `userData.animate(dt, {state, moving})` + `playGesture` + `outlineScale` |
| 对话 | 解释器、信号、效果、`DialoguePanel` 全套现成 |
| 面板栈 | Redux `uiSlice.panelStack` + `usePanel` / `useMirroredPanel` + `EscArbiter`（ESC 的唯一裁判） |
| 世界坐标 → 屏幕像素 | `RoomScene.getHintBubble()` + `InteractBubble` 的 rAF + transform 骨架，进度条照抄 |
| 花钱 | `spendGoldFrom(amount)`（金币存在罐的 `state.stored` 里，罐就是钱包） |

### 空的 / 缺的

| 缺口 | 现状 |
|---|---|
| **建造代价** | **字段不存在**。`BuildingLevel` 只有 `upgradeCost`，新建一栋楼永远免费 |
| **扣材料** | `checkUpgrade` 的 `missing_materials` 校验写好了，但 `upgradeBuilding` 调它时**不传 `materials`**（`buildings.ts:266`），这道门物理上无法触发；**扣材料的代码根本不存在** |
| **施工时间** | `construction { targetLevelId, finishUtc, workerId? }` 和 `buildDuration` 都是预留字段，**全仓库零引用**。Core 里没有任何推进/结算施工的函数。而且只有 `finishUtc`，**算不出进度百分比** |
| **工人** | 只有 `workerId?: string` 一个字符串。没有工位、没有认领/释放、没有排队 |
| **围栏 / 半成品** | `BuildingsView.applyState` 只认 `fill`（液面）和 `stage`（农田），没有在建表现 |
| **图纸** | 零。能建什么是硬编码数组 `buildableIds()` |
| **建造 UI** | **唯一入口是控制台指令**。玩家点地上的罐子毫无反应——`refreshInteractTarget` 只遍历家具 + 门 + 宠物，**从不看 `listBuildings()`** |
| **NPC** | 不存在这个概念，只有宠物。`storyRules` 和 `dialogueDefinitions` **都是空数组** |
| **进度条** | 没有任何可复用组件，也没有世界空间进度条 |

---

## 分五期

每期结束都可玩、可验收、单独一笔提交。

### 期 A · 石傀儡到场：无头休眠 → 装头 → 苏醒

**Core**
- `Core/src/types/pets.ts` 加 `role?: "pet" | "worker"`（缺省 `"pet"`，老档零迁移）
- `Core/src/Data/pets/index.ts` 加 `stone_golem`：`role: "worker"`、`collisionRadius: 1.1`、`moveSpeed: 0.55`（石头做的，比舒舒还慢）
- `Core/src/Data/items/index.ts` 加 `golem_head`：`placement.interactHint = { action: "pickup" }`

**Frontend**
- `Game3D/Visual/recipes/golem.ts`（新）：低多边形石傀儡，照 `shushu.ts` 的 pivot 骨架，但**走平面着色**（岩石本来就该有折面，和 `soft()` 的毛绒路线相反）。可动件：`headSocket`（空的脖颈插槽）/ `armLeft` / `armRight` / `legs[2]` / `body`。`userData.animate` 三态：`sleeping`（整体下沉、苔藓面朝上）、`idle`（呼吸似的起伏）、`work`（双臂交替砸下去）
- `PetAgent`：`role === "worker"` 时 `chooseNextActivity` 跳过吃/喝/亲近三支
- 开局把它 `placeSleepingPet` 在门前院子东侧；头摆在院子西北角（详见坐标表）
- **装头交互**：手上拿 `golem_head` 对着它按 F。照唱片机那条路写（`RoomScene.ts:1583` 的"拿着唱片 → 换碟"），不走送礼系统

**验收（2026-08-22 实测，全部通过）**

| 判据 | 实测 |
|---|---|
| 开新档：院子东侧一尊无头石傀儡坐着 | `pet-stone_golem` 在 (1.5, 8)，`state: sleeping`、`dormant: true`、`parts: []` ✅ |
| 头在院子另一头 | `golem_head@yard`，世界 (−12.5, 12.5)，房子西侧 ✅ |
| 走到头旁边有气泡 | `getHintBubble()` → `hint.golem_head`（动作 pickup = 右键）✅ |
| 捡起来进背包 | `pickupFurniture` → 手上 `golem_head` ✅ |
| 走到傀儡跟前，气泡随手上东西换词 | 拿着头 → `golem.hint.attach`；空手 → `golem.hint.dormant`（不给按键标签）✅ |
| 按 F 装上、傀儡站起来 | `interactTarget.kind: "pet"` → `parts: ["head"]`、`dormant: false`、`state: idle`，手上清空 ✅ |
| 叫不醒 | 缺零件时 `wakeUp()` 是空操作（用例钉住）✅ |
| 干活的不吃不喝 | 饱食/水分归零后推一帧，不会进 `eat` / `drink` ✅ |
| 存档往返 | 装过头的读档还是醒的；没装的读档还是瘫的 ✅ |
| 老存档兼容 | 没有 `attachedParts` 字段的宠物按"零件齐全"算，不会集体瘫 ✅ |

**做的时候撞出来的三件事（都记在注释里）**

1. **模型第一版读成一根石柱**。三块 1.5 宽的石板越堆越窄、手臂缩在轮廓
   里面，站起来从远处只看得见一个长方体。人形的辨识度几乎全在剪影上：
   肩要比腰宽出去一截、头和肩之间要留出脖子、手臂要挂在轮廓**外面**。
   重排比例之后才读得出是个人形。坐姿同理——第一版沉 0.62、倾 0.3，
   读出来是一堆塌掉的石头；坐着的人形要保住竖直的躯干。
2. **`interactTarget` 两把尺子**（真 bug，顺手修了）。家具算的是到占地
   矩形最近边，生物算的是到中心。半径 1.1 的石傀儡贴着站中心距 1.3，
   旁边一米开外的纸箱按边算只有 1.2——按 F 打开的是纸箱，而玩家眼里
   那两样东西的远近正好是反的。生物那侧改成减掉体型。
3. **物品自检抓住一个空话字段**。头第一版写了 `surfaceHeight: 0.5` 却
   `blocksMovement: false`，`itemAudit` 当场报"有台面高度的必须挡路"——
   不挡路的家具上面放东西会直接落地，台面高度是句空话。

**期 A 的提交**：`（见 git log）`

### 期 B · 按 F → 建造面板 → 图纸（**没有对话**）

> G10 把这一期砍掉了一大半：不写 `storyRules`、不写 `dialogueDefinitions`、
> 不加 `StoryEffect`。醒着的石傀儡按 F 直接 `emit("build_shop_open_requested")`。

**Core**
- `LevelShape` / `BuildingLevel` 加 `buildCost?: Array<{ itemId, quantity }>`
- `logic/buildings.ts` 抽出 `missingMaterials(cost, have)`，`checkUpgrade` 和新的
  `checkBuildAfford` 共用——两处各写一份判断迟早走散
- `ItemDefinition` 加能力块 `blueprint?: { buildingId }`；新物品 `blueprint_gold_jar`
- gold_jar l1 加 `buildCost: [{ itemId: "gold", quantity: 0 }]`

**Frontend**
- `Game/Systems/materials.ts`（新）：`materialCounts` / `canAfford` / `spendMaterials`
- 新面板 `Components/BuildShopPanel/`，`PanelId` 加 `"buildShop"`
- `RoomScene`：`interactTarget` 是**醒着的 Worker** → 开面板

**金币怎么混进材料数组**（这一期唯一的设计难点）

钱不在背包里（它是金币罐实例的 `state.stored`，"罐就是钱包"），但用户要
材料是**一个数组**。与其给每个代价表配一个 `goldCost: number` 的旁路字段，
不如让金币占一个**保留 id** `"gold"`：面板只渲染一张表，扣费只走一条路，
以后加"圣水""声望"这类非背包资源也是照这里加一行。Core 那层因此完全不
需要知道钱存在罐子里这回事——它只收一张"你现在有多少"的清单。

代价是 `"gold"` 不能是真物品的 id。`auditMaterials()` 开机拦这个。

**上架清单 = 有没有图纸物品**

不另写一张"商店卖什么"的表：注册表里存在 `blueprint.buildingId` 指向它的
物品，它就上架。加一种可盖的建筑 = 加一件图纸物品，面板一行不用改；
反过来也不会出现"面板上有但买了没用"的条目。

**验收（2026-08-22 实测，全部通过）**

| 判据 | 实测 |
|---|---|
| 对着醒着的傀儡按 F | `interactTarget.kind: "pet"` → 面板弹出，标题「要盖点什么」✅ |
| 面板列出储金罐 + 逐条材料 | 「金币罐 / 金币 0 (0)」，够 → 绿字 ✅ |
| 点「要图纸」 | 背包多一张 `blueprint_gold_jar` ✅ |
| 0 枚就是免费 | 一无所有也 `canAfford` ✅（用例钉住） |
| 扣材料全有或全无 | 金币够、木板不够时，**金币一分不动** ✅ |
| 金币从罐读不从背包读 | 没罐时 `materialCounts().get("gold") === 0`；`getCount("gold") === 0` ✅ |
| 保留 id 不撞名 | `auditMaterials()` 空 ✅ |

### 期 B 附带：游荡 AI 和两个 bug

用户看着他站着不动、还浮在空中，补了一句"他有自己随便走来走去的简单 AI，
你可以先做一版……还有他浮在空中不知道为什么"。两件都是真 bug：

1. **浮空**：`golem.ts` 的 `animate` 里写了 `root.position.y = 0`，把
   `PetView` 每帧设的**地面高度**覆盖掉了。院子地面在 −0.45，所以他正好
   浮起 0.45。PetView 的约定写得很清楚："内部只动自己的子节点，root 的
   位置朝向仍归这里管"——加一个 `rig` 中间节点就守住了。
2. **不会走**：`PetAgent` 的寻路（`randomFreeCell` / `startPath` / 坐标换算）
   全都取 `getWorld().room`，也就是**主房间**。石傀儡站在院子里，格号换算
   直接算到房子的网格上去了，每次算路都失败。这是"一图一屋"公理在生物
   行为里最后一份拷贝，院子在期 1 变成真房间之后一直没人踩到——宠物从来
   只在屋里。改成按**脚下那个房间**算（`space()`）。

顺带两条：
- **驻地半径**（`PetBehavior.wanderRadius`，石傀儡 5 米）。院子是一整块
  60×45 的房间，不限半径他会一路溜达到据点另一头。驻地**进存档**，
  不能读档时拿当时站的位置顶上——那样每存读一次驻地就朝他溜达到的地方
  挪一次，挪几回就漂没了。
- **采样要先收到驻地附近**。60×45 = 2700 格里 5 米的圆只占 79 格，全房间
  均匀抽的命中率不到 3%，抽 24 次有一半机会一个都中不了——表现是他走一段
  就呆站十几秒。另外候选点要过 `isWalkable`（那条才认领地），不然他会
  径直走进没解锁的地里去。

### 期 C/D/E · 工地、施工、管理面板（一起做完）

**期 C · 图纸 → 蓝图 → 工地**

- Core：`construction` 补 `startUtc`，而且和 `finishUtc` **都改成可选**——
  排队中的工地两者皆无。加 `constructionProgress` / `isConstructionDone` /
  `isConstructionQueued`
- `placeBuilding(..., { asSite: true })`：落下去的是工地不是成品。工地就是
  这栋建筑的一个阶段，不另立实体——占地校验、迁移、拆除全部立刻复用
- 手上拿着图纸按 F → `beginBuildingSiting`。**选址控制器一行没改**
- 图纸**确认成功才消耗**：那块地不能放时图纸留在手上。扣了钱又没盖成
  是最难解释的一种失败
- `Buildings/site.ts`：半透明成品 + 一圈施工围栏。两样各干各的活——
  半透明说"将来会是什么"，围栏说"这块地被占了"（半透明的东西在远处几乎
  看不见，光靠虚影读不出一块地已经被下单）
- `Components/BuildProgress`：世界空间进度条，照 `InteractBubble` 的
  rAF + transform 骨架。**每帧动的是 style 不是 state**——位置和百分比每帧
  都变，走 setState 的话一块工地就能让整棵 UI 树每帧重渲染

**期 D · 石傀儡施工（单工位排队）**

- `PetActivity` 加 `work`，`Errand` 加 `{ kind: "build" }`
- `trySeekSite()`：**手上已经有活就接着干那块**，不会半路改主意——那正是
  "建 A 的时候 B 不会动工"。挑**最早下单**的（先来先建是玩家唯一能预测的
  规则；按距离挑的话下单顺序和开工顺序对不上，看着像随机）
- 走到工地边 → `claimSite`（**这一刻才写开工时刻**）→ 播砸击动画 → 到点
  `finishSite` → 立刻找下一块
- 被引开 / 读档 / 傀儡没了 → `releaseSite`，工地退回队列。`workerId` 留着
  的话那块地永远等一个不存在的工人，队里后面的跟着卡死

**期 E · 建成后的管理面板**

- `refreshInteractTarget` 加 `{ kind: "building" }` 一支（照门那支抄）。
  距离算到**占地矩形最近边**，和家具那把尺子一致——4×4 的罐子按中心算
  得走进它身体里才够得着
- `Components/BuildingPanel`：概览 / 升级 / 迁移 / 拆除。迁移和升级都要选
  位置，所以不在面板里执行——发意图给场景，由两步确认接管
- `upgradeBuilding` **这次真的传材料了**：以前不传，`checkUpgrade` 里那道
  `missing_materials` 的门物理上永远开着，校验写好了却从没被触发过
- 升级**不再瞬间完成**：下单变工地，`levelId` 要到完工才落。在建期间这栋
  楼仍是旧等级，容量和内景照样用得上

## 这条链上最容易写错的一处

`finishUtc` 是绝对时间。要是**下单时**就按墙钟算，玩家关掉游戏睡一觉回来，
排在后面的工地会全部自己建好——单工位就白设了。

数据上的保证是：**没有 `workerId` = 没有 `startUtc` = 进度恒 0**。
时刻在工人认领的那一刻才写，所以那件事在数据上就不可能发生，不需要在
别处补一层判断。`construction.test.ts` 里"排队的工地放一万年也不会自己
建好"钉的就是它（判据用 `9999-12-31`）。

## 验收（2026-08-22 实测，全部通过）

**九条命令全绿**：Core 344 / Frontend **381**（新增 8 条施工用例）。

期 2 的升级用例要跟着改：`upgradeBuilding` 不再瞬间完成，那些用例钉的是
**升级规则本身**（分叉、非空不给升、instanceId 不变），不是施工流程，
所以统一走一个 `upgradeNow` 辅助（下单 + 就地完工）。

**浏览器整条回路**（清档新开）：

| 步骤 | 实测 |
|---|---|
| 捡头 → 装上 → 苏醒 | `dormant: false`、`state: idle` ✅ |
| 按 F | 建造面板弹出（`要盖点什么`）✅ |
| 买图纸 | 扣材料成功，背包多一张 ✅ |
| 拿着图纸按 F | 进入选址（`currentMode: "build"`）✅ |
| 确认 | 工地落地 (−5, 0)、图纸消耗、`queued: true` ✅ |
| 石傀儡自己走过来 | 从 (1.5, 8.6) 走到 (−3.8, −0.5)，`claimed`、`state: work` ✅ |
| 20 秒后 | 工地清空、罐子成品、傀儡回去游荡 ✅ |
| 两块工地并存 | 只有一块 `claimed`，另一块 `queued` 且进度恒 0 ✅ |
| 围栏 + 半透明成品 | 都渲染正确 ✅ |
| 进度条 | DOM 里两条，文案`等着开工` ✅ |
| 对着建好的罐按 F | 面板四项齐全：概览（存了 0/50）/ 升到「加固」/ 迁移 / 拆除 ✅ |

> 中途有一次"幽灵不渲染"的虚惊：是**探针自己传错了参数**——
> `placeBuilding(..., 0, ...)` 里的 `0` 不是 `Facing` 枚举值，旋转矩阵成了
> NaN，整个节点被视锥剔除。游戏代码没问题，记在这里免得下次再查一遍。

## 刻意不做的

- 多个傀儡 / 多工位（规则天然支持，本期只有一尊）
- 建造队列的手动排序 / 退单
- 傀儡在施工间隙的复杂 AI（现在是驻地 5 米内游荡）
- 拆除返还材料

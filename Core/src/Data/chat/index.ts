/**
 * 消息流保留多久。
 *
 * 放注册表而不是写死在裁剪函数里：AGENTS.md 禁止在 gameplay 代码里
 * hardcode 平衡数值，"留几天"就是一个平衡数值——它是在"翻得回去"和
 * "存档别无限涨"之间取的一个点，以后会调。
 */
export const chatLogRetention = {
  /** 保留最近几个**世界日**（含今天）。3 = 今天 + 昨天 + 前天 */
  days: 3,

  /**
   * 条数上限，天数之外的第二道闸。
   *
   * 只按天数会有一个洞：挂机一整天、每秒刷几条命令反馈，同一天之内就能
   * 堆出几万条，而它们一条都不过期。存档大小不该由玩家手速决定。
   */
  maxMessages: 500,
} as const;

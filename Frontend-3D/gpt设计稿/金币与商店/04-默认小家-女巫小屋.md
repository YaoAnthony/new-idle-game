# 默认小家 · 女巫小屋（LV1）

> 状态：**已完成** · 2026-08-22 · 前置：期 0–2 全部完成 · 三笔提交见文末
>
> 标准同前三期：任何人不看聊天记录也能照着做完并验收。

## 拍板过的（2026-08-22，用户定）

| # | 决策 |
|---|---|
| H1 | 占地 **9 宽 × 12 深**（长宽比 3:4），放在开局格 C3 里 |
| H2 | **走地图自带的主房间**那条路：`generateRooms` 直接生成 LV1 户型当 `living`，**开局不收起**。推翻期 1 的 T9「房子默认收起」 |
| H3 | 户型：洗手间 3×3（西北，带门）、卧室 5×5（东北，带门）、玄关 2×2（南门）、其余 LDK |
| H4 | 大门开在**南墙**，面朝 C3 中心和院子 |
| H5 | 外观照参考图：**女巫帽屋顶**——峰在西、长坡往东扫一道弧、烟囱在东坡；乱石墙；菱格小窗；木板门 + 小雨棚 |
| H6 | 墙高 3.2 / 峰高 8。屋里天花仍是 3.2 的平顶，尖帽是外观 |
| H7 | `Game3D/World/House/` 的 2LDK 户型（`generateHouse`）从此算 **LV3**。渲染器本身是通用的，不复制 |

## 两条硬前置

### 前置 A · 可走范围和房子尺寸解耦

`yardBoundsOf(map, room.floorGrid)` 拿**房子宽深 + 四向边距**算可走范围，三处消费（`doorsRuntime`、`navigation`、镜头）。房子从 24×20 缩到 9×12，可走范围跟着缩：

| | 24×20 | 9×12 | 后果 |
|---|---|---|---|
| 东界 | 12 + 33 = **45** | 4.5 + 33 = 37.5 | 东桥（到 45）走不到头 |
| 西界 | −12 − 24 = **−36** | −4.5 − 24 = −28.5 | A 列领地（−40..−25）出界 |

**改法**：`MapDefinition` 加可选 `walkableRect?: DeckRect`，给了就直接用，不再从房子推。base 写今天的实际值 `{ minX: -36, maxX: 45, minZ: -24, maxZ: 47 }`——**零行为变化**，只是把"据点多大"从房子身上摘下来变成地图自己的数。小镇、店铺不动。

```ts
// Core/src/logic/roomGeometry.ts · yardBoundsOf —— 改后
export function yardBoundsOf(
  map: Pick<MapDefinition, "yardMargin" | "yardMargins" | "walkableRect">,
  floorGrid: { width: number; height: number },
): DeckRect {
  // 地图自己声明了可走范围就用它——"据点多大"是地理，不该跟房子尺寸走。
  // 老的按房子推的算法留给没声明的图（小镇、店铺），它们的房间就是整张图
  if (map.walkableRect) return map.walkableRect;
  …原逻辑不动…
}
```

验收：`Core` 加一条用例——给 `walkableRect` 时返回它、不给时按原算法；Frontend 现有 `yardBoundsOf` 的三处调用方不改。

### 前置 B · 出生点、开局摆设、围栏都要跟着新房子走

| 什么 | 现在 | 改成 |
|---|---|---|
| `spawn` | `(−2, 10)` 院子里（T9 时代：房子收着，站空地） | 玄关内侧、面朝门，**房本地坐标**经 `spawnWorldOf` 世界化 |
| 房子锚点 | 无（原点） | `{ x: -2.5, z: 9 }`：占地 x −7..2 / z 3..15，落在 C3（x −10..5 / z 3..18）里，南面留 3 格给门廊和出生点 |
| `seedInitialFurniture` | 纸箱在 `(2,3)`/`(4,4)`——那是 2LDK 的格 | 挪到 LDK 里（9×12 里 `(2,3)` 是洗手间） |
| `outdoorDecks`（缘侧） | 24×20 的房本地坐标 | **删空**。女巫小屋没有缘侧；这张图的缘侧是和风那栋的 |
| `territoryAudit` 的出生点校验 | 出生点在 initial 格内 | 世界化之后仍在 C3 内（玄关在 z≈14，C3 到 18） |

## 户型数据（`Maps/base/layout.ts` 新增 `generateCottageL1`）

```
      x0  1   2   3   4   5   6   7   8
 y0  ┌───────────┬───┬───────────────────┐  北墙
 y1  │  洗手间   │墙 │    卧室 5×5       │
 y2  │   3×3     │   │                   │
 y3  ├───┬门┬───┤   │                   │
 y4  │           │   │                   │
 y5  │           └───┴───┬门┬───────────┤  内墙行 y=5（x4..8）
 y6  │                                   │
 y7  │         L D K   （灶台靠东墙）     │  东墙外立烟囱
 y8  │                                   │
 y9  │                                   │
 y10 │   ┌玄关┐                         │
 y11 └───┤ 门 ├─────────────────────────┘  南墙 = 正面山墙
```

- 内墙：列 `x=3` 从 y0 到 y5（6 格，隔洗手间|卧室和 LDK）、行 `y=3` 的 x0..2（洗手间南墙，门在 x1..2）、行 `y=5` 的 x4..8（卧室南墙，门在 x5..6）
- 门：两扇 `room_door`（复用 2LDK 的门定义和 `RoomDoorView`）
- 大门：南墙 `gridPosition { x: 1, y: 0 }` 宽 2 高 2；玄关分区 `{ x: 1, y: 10, w: 2, h: 2 }`
- 窗：北墙一扇（卧室）、东墙两扇（LDK 灶台两侧）、西墙一扇（LDK）——**菱格小窗**，照图
- 分区：`genkan` / `bath` / `bedroom` / `ldk` 兜底。`Bath` 分区自动铺瓷砖地（HouseBuilder 已有）
- 墙高 3.2（`walls.*.grid.height`）

## 外观（新做的四样，都挂在**风格**上）

`RoomStyleDefinition.visual` 加一项 `shell: "wafu" | "witch_cottage"`。`HouseBuilder.buildHouse` 按它选屋顶 / 外皮 / 大门 / 门廊的建造器——**一条管线两套皮**，地板、天花、内墙、门洞、拾取面、窗玻璃照旧共用。新风格 `witch_cottage`（`Core/Data/roomStyles`），`forest_cottage` 等老风格默认 `wafu`，行为不变。

| 新文件 | 做什么 |
|---|---|
| `House/WitchRoof.ts` | 女巫帽：屋脊沿 z（南北向）在 x≈1.5，峰高 8。西坡陡直到西檐；**东坡是一道弧**（二次曲线采样 6–8 段）扫到东檐，檐口向外挑 0.6。南北两面山墙填石。木瓦：按行错缝的四边形 + 抖色，合批成一个 `createQuadMesh` |
| `House/StoneWalls.ts` | 乱石外皮：逐格四边形，每格在灰米色基色附近抖、随机分成 1–3 块"石头"（用 `hash01` 定）。只建朝外的面，和 `ExteriorWalls` 同样的"外皮往外挪 0.12 + 分组即淡出单位" |
| `House/Chimney.ts` | 石砌烟囱：东坡靠屋脊，方柱 0.9×0.9，顶出屋面 1.6，顶上一圈压顶石。位置从 `WitchRoof` 的坡面函数算，**贴坡不悬空** |
| `House/PlankDoor.ts` | 木板门 + 小雨棚：五块竖板 + 两条横档 + 铁铰链色；门上方一片 1.4 宽的木瓦小雨棚，两根斜撑。合页在左、往外开（女巫小屋不是引き戸）。开合动画照 `DoorView` 的接口，`doorsRuntime` 一行不改 |
| 窗样式 `window_lattice` | 菱格：玻璃上铺一层斜 45° 的细木条（8–10 根），木框加厚。挂在 `WindowView` 的 visualId 分支上 |
| 布景 | 门两侧各一丛蘑菇（`blob` + `cylinder`）、墙脚几簇野花——挂在房子 root 下随锚点走 |

**不做的**：屋里挑高（H6 定了平顶）、L 形体量（墙系统只支持矩形，用屋顶剪影代替）、缘侧。

## 期 2 建筑实例那栋 `house` 怎么办

`/build house` 盖出来的是期 2 的粗盒子模型，而现在默认的家是地图自带的。两栋并存会出现"领地上有两个家"。**本期**：把 `house` 从 `buildableIds()` 里摘掉（型号定义留着，`maxInstances: 1` 不动），`/build house` 报"家已经有了"。合并成一条路（默认家 = 建筑实例 l1、升级到 l3 = 2LDK）是下一期的事——那一步要让建筑实例的内景也走 `HouseBuilder` 渲染，期 2 文档「刻意没做」里记了。

## 存档

- `RoomSave` 形状不变（仍是 walls / interiorWalls / zones），**不 bump 版本**
- 老档里的 `living` 是 24×20 的 2LDK 几何（存结果不存配方）——读进来**照旧是 2LDK**，不会自动变成小屋。测试阶段照 T10 清档；真要迁移是另一期
- `outdoorDecks` 删空后老档的缘侧消失（它是地图定义不是存档），这是预期

## 做下来和计划不一样的地方（都是做的时候才知道的）

| 计划 | 实际 | 为什么 |
|---|---|---|
| H6 墙高 3.2 | **墙高 3** | `WallSave.grid.height` 是墙格数（整数行），3.2 不是合法值。屋内镜头 `maxY` 因此是 2.65 |
| `RoomStyleDefinition.visual.shell` | **`RoomSave.shell`**（Core `HouseShellKind`） | 外壳是**户型**的属性不是**风格**的：同一个 `forest_cottage` 风格既要给 LV3 的 2LDK（和风）也要给 LV1 的小屋（石屋）用。放在风格上就得为每种外壳复制一套风格。`generateCottageL1` 直接写 `shell: "witch_cottage"`；没写的房间（老档、2LDK、店铺）默认和风，行为不变 |
| `House/Chimney.ts` 独立文件 | 并进 `WitchRoof.ts`（`buildChimney`） | 烟囱落点要从屋面剖面函数采样，和屋顶是同一份几何 |
| 烟囱在壁炉正上方（东墙） | **z 跟壁炉行，x 挪到东坡脊檐之间 45%** | 东墙正上方就是东檐，屋面在那儿只有墙高，烟囱整根成了贴在墙外的石柱。壁炉行从 LDK 分区中行推（`HouseBuilder.chimneyAnchor`），开局壁炉也按同一规则摆到 `(7, 6)` |
| 缘侧 | 无 | 计划就说删。女巫小屋没有缘侧 |
| 洗手间有洁具 | **空房间 + 瓷砖地** | 家具表里没有任何洁具；加家具是内容不是本期 |
| 木瓦隔行错半格 | 通长条、不错缝 | 错半格会在相邻两行之间留半格的洞（第一版从院子里能透过屋顶看见树） |

顺手修的一个**老 bug**（寻路验收第 6 组抓到的）：`findRoute` 不管目标站不站得住，最后一个路点一律用真实坐标；目标在北墙外半米时，角色走到屋里贴墙那格之后**再穿墙挪到目标点**。改成目标格不可走就停在吸附格的格心（`navigation.ts`），`cottage.test` 两条用例钉住。

## 验收（2026-08-22 实测）

**headless（Core）** — `cd Core && npm test`：全过（含 `roomGeometry.test` 的 `walkableRect` 两条）
- [x] `yardBoundsOf` 给 `walkableRect` 时原样返回；不给时按原算法
- [x] `generateCottageL1` 过 `roomAudit`；`territoryAudit` 出生点在 C3（`main.tsx` 启动审计，控制台无报错）

**vitest** — `cd Frontend-3D && npx vitest run`：22 文件 356 条全过
- [x] `cottage.test`：9×12、墙高 3、南门 + 两扇房门；不 stowed；整个落在 C3；出生点在玄关面朝门；108 格脚印；瓷砖分区
- [x] `defaultPlacementRoom()` 回到 living
- [x] serialize 往返（`serialize.test`）
- [x] 寻路：目标站不住时停在吸附格；站得住时用真实坐标

**浏览器（5183，清档开新档）** — 探针脚本在会话 scratchpad `boot.js`；走路用 `findRoute` + `controller.walkAlong`，每组记录起终点实际坐标
| # | 路线 | 目标 | 实际到达 | 结果 |
|---|---|---|---|---|
| 1 | 院子 → 玄关（过南门） | (−5, 13.5) | (−5, 13.51) | ✅ |
| 2 | 玄关 → 卧室（过卧室门） | (−0.5, 5.5) | (−0.48, 5.56) | ✅ |
| 3 | 卧室 → 洗手间（两扇门） | (−5.5, 4.5) | (−5.5, 4.53) | ✅ |
| 4 | 洗手间 → 灶台旁 | (−0.5, 9.5) | (−0.51, 9.5) | ✅ |
| 5 | 灶台旁 → 东桥头（领地外） | (30, −4) | `findRoute` 返回 null | ✅ 领地拦住 |
| 6 | 灶台旁 → 北墙外半米（锁定格） | (−2.5, 1.5) | 路终点 (−0.25, 3.75)，在卧室贴北墙 | ✅ 修前会穿墙到 (−2.47, 1.53) |

- [x] 出生在玄关内侧面朝南门；镜头盒 `living`，`maxY` 2.65（墙高 3 − 0.35）
- [x] 四面外皮无洞（南/东/北/西 + 东北俯视截图）；从南面看峰在西、弧坡往东扫；烟囱在东坡
- [x] 南门木板门往外开（铰链转 −1.76 rad），关上归零；卧室门 `interact()` 开/关；洗手间瓷砖地
- [x] 九条验收命令全绿：Core `npm test` / `tsc -p tsconfig.json --noEmit` / `tsc -p tsconfig.test.json`，Backend 同三条，Frontend `vitest run` / `tsc -p tsconfig.app.json --noEmit` / `tsc -p tsconfig.test.json`

## 提交

① `4804372` 前置 A（`walkableRect`，零行为变化）；② `4cc63cc` 户型 + 出生点 + 开局摆设 + 测试；③ 外观（`WitchRoof` / `StoneWalls` / `PlankDoor` / 菱格窗 / 蘑菇野花）+ `RoomSave.shell` + `/build house` 摘掉 + 寻路终点修正。

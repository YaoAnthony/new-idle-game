# 期 5 · 家具小店

> 状态：**待施工** · 2026-08-24 · 前置：期 3（卖货成立）+ 期 4（三位居民住齐）
>
> ⚠️ **要建一栋店铺——动手前先问用户要参考图**（总纲纪律）。

## 一句话

三位邻居来问能不能买你的家具，于是你开了间小店：把家具摆上货架，
关掉游戏，第二天回来变成钱和一句「谁买走了它」。

**从"等水獭来"变成"天天都能卖"——这才是小店真正的价值。**

## 拍板过的

| # | 决策 | 出处 |
|---|---|---|
| 15 | 满 3 位居民 → 他们求购 → 你造一间家具小店，行动得来的家具摆上去卖 | 用户 |
| 21 | **挂机式**：东西放进去，客人自己来买，隔天结账 | 用户（餐厅同款，这里先立骨架） |
| — | 水獭不在时家具堆着**是想要的压力**，小店就是解法 | 用户 2026-08-24 |

---

## 这一期立的是**骨架**，餐厅（期 8）是第二个用户

小店和餐厅是同一件事的两个皮：

| | 家具小店（期 5） | 自家餐厅（期 8） |
|---|---|---|
| 上架什么 | 家具 | 做好的菜 |
| 谁来买 | 居民 | 居民（以后加小镇散客） |
| 什么时候结算 | `day_started` | `day_started` |
| 结果 | 金币 + "谁买走了什么" | 金币 + "昨晚谁来吃了什么" |
| 报纸版块 | 市场行情 | 餐厅 |

**所以这一期要把 `Systems/shopkeeping.ts` 写成通用的**：一个"货架 →
隔夜结算 → 报告"的机器，小店和餐厅各自喂它不同的货和不同的客源。
期 8 的工作量因此会小很多。

写的时候就按两个用户设计，不要等期 8 再来抽象——那时候第一个用户已经
长满了小店专属的假设，抽象会变成重写。

---

## 现状盘点

### 现成的

| 要的 | 现成的 |
|---|---|
| 建筑链 | 图纸 → 选址 → 石傀儡施工 → 完工 → 管理面板（期 2/石傀儡那期做完） |
| 实例状态 | `BuildingPlacement.state`（金库的 `stored`、农田的播种时刻都住这儿），**进存档** |
| 完工信号 | `building_completed`（期 0） |
| 每日结算的钩子 | `day_started`（期 0） |
| 昨日事实 | `Systems/dayRecord.ts`（期 0），`recordGoldFact` / `recordHeadlineFact` 都留好了口 |
| 价格 | `ItemDefinition.value`（期 1） |
| 居民名单 | 期 4 的 `CreatureRole.Resident` |
| 预算 | `economy` 表的 `shopkeepingTuning.budgetPerResidentPerDay` |
| 确定性抽签 | `hashSeed` / `drawDeterministic` |
| 金币入账 | `depositGoldTo`（走溢出规则） |
| 面板栈 | `PanelId` 加一个 + 组件；`StoragePanel` 是"往格子里放东西"的现成参照 |

### 空的

| 缺口 | 现状 |
|---|---|
| **店铺模型** | 零 |
| **货架槽位** | 零 |
| **上架面板** | 零 |
| **隔夜结算** | 零。全游戏还没有任何"离线期间产出"的东西（农田是按时间戳算阶段，不是结算） |

---

## 分三步

### 5A · 求购对话 + 店铺建筑

（期 4 留下来的那条规则挪到这里，玩家不会拿着一张暂时盖不了的图纸。）

```ts
{
  id: "residents_want_furniture",
  triggers: [{ signal: "resident_moved_in", signalCount: 3 }],
  effects: [
    { kind: "start_dialogue", dialogueId: "residents_ask_for_shop" },
    { kind: "give_item", itemId: "blueprint_furniture_shop", quantity: 1 },
  ],
},
```

`Buildings/furnitureShop.ts`（新，**先问参考图**）：

```ts
export const furnitureShop: BuildingDefinition = {
  buildingId: "furniture_shop",
  localizationKey: "building.furniture_shop",
  doorOffset: 0,
  maxInstances: 1,
  levels: [
    {
      levelId: "l1",
      footprint: { width: 4, height: 4 },
      // 走进去看得到货架——同图内景，和居民房、landCabin 同一条路
      interior: (style) => buildInterior({ width: 4, depth: 4, windows: true }, style),
      // 升级 = 更多货位（见 5B 的 slotsOf）
      nextLevelIds: ["l2"],
      build: () => /* ... */,
    },
    { levelId: "l2", footprint: { width: 5, height: 5 }, /* ... */ },
  ],
};
```

**升级换的是货位数**，这是店铺唯一有意义的成长维度——`upgradeCost` 走
`economy` 表。

### 5B · 通用的「上架 → 隔夜结算 → 报告」骨架

`Systems/shopkeeping.ts`（新）。**这个文件从第一天就要为两个用户写。**

```ts
/**
 * 挂机式经营的通用骨架：**上架 → 隔夜结算 → 报告**。
 *
 * 两个用户：家具小店（期 5）和自家餐厅（期 8）。两者的差别只有三样——
 * 货是什么、客源是谁、报告怎么写——所以它们是这个机器的**参数**，
 * 不是两份代码。
 *
 * ## 为什么是挂机式而不是柜台式
 *
 * 用户 2026-08-24 定的。而它恰好是本作**唯一**和"绝对时间结算、关掉
 * 也在走"对得上的形态：你把货摆上，关游戏，明天回来钱到账。柜台式
 * （客人上门你现卖）要求玩家在场，和整个定位拧着。
 *
 * ## 货架就是天然的上限
 *
 * 离线十天回来不会凭空多出十天的钱——**货架上有多少就卖多少，卖完就停**。
 * 这正是动森那条"产出补但封顶"（跳一天化石 4→6，跳十天还是 6），
 * 而我们连封顶规则都不用写：货架自己就是上限。
 */

export type Shelf = {
  /** 哪栋楼的货架 */
  instanceId: string;
  /** 货位。null = 空位 */
  slots: Array<{ itemId: string; count: number } | null>;
};

export type ShopKind = {
  /** 这类店卖什么（判据函数，不是清单） */
  accepts: (itemId: string) => boolean;
  /** 今天有哪些客人，各有多少预算 */
  customers: () => Array<{ id: string; budget: number }>;
  /** 一件货对某位客人值多少（餐厅要按品质加价，小店就是 value） */
  priceFor: (itemId: string, customerId: string) => number;
  /** 结算报告怎么写进昨日事实 */
  report: (sold: SoldEntry[]) => void;
};

/**
 * 结算一天。**确定性**——同一天同一份货架同一批客人，算出来必须一样，
 * 否则玩家重开游戏就能重摇销量。
 *
 * 顺序：客人按 id 排序 → 每人按预算从贵到便宜挑（贵的先被买走，
 * 这样"摆好货"是有回报的）→ 卖完即停。
 */
export function settleDay(shelf: Shelf, kind: ShopKind, worldDayId: string): SoldEntry[];
```

**小店的 `ShopKind`**：

```ts
const FURNITURE_SHOP: ShopKind = {
  accepts: (itemId) => Boolean(findItemDefinition(itemId)?.placement),
  customers: () =>
    listResidents().map((r) => ({
      id: r.id,
      budget: shopkeepingTuning.budgetPerResidentPerDay,
    })),
  priceFor: (itemId) => findItemDefinition(itemId)?.value ?? 0,
  report: (sold) => {
    recordGoldFact(sold.reduce((sum, s) => sum + s.price, 0));
    for (const s of sold) recordHeadlineFact("shop_sold", s.itemId);
  },
};
```

**客源做成函数不是数组**（用户 2026-08-24 定："我们可以默认是居民先"）：
以后加"小镇散客""节日客流"是往名单里加来源，结算逻辑一行不动。

### 5C · 上架面板 + 离线补算

#### 面板

`PanelId` 加 `"shopShelf"`；组件照 `StoragePanel`（那也是"背包 ↔ 格子"
的双向搬运），加一列"这件值多少"。

#### 离线补算

```ts
// day_started 的监听里
const missed = daysBetween(lastSettledWorldDayId, currentWorldDayId);
for (let i = 0; i < missed; i += 1) {
  const sold = settleDay(shelf, kind, dayIdOf(i));
  if (sold.length === 0) break;   // 货卖完了，后面几天不用算
}
```

**按实结算多天**（总纲的"产出类：补，但封顶"），封顶靠货架存量。
`lastSettledWorldDayId` 存在建筑的 `state` 里。

> **不要给它加一个"最多补 N 天"的上限。** 货架就是上限，再加一层
> 是在惩罚离线——而离线补算封顶 12 小时那条规矩管的是**消耗**
> （饿、累），不是产出。产出没有理由罚。

---

## 最容易写错的四处

### 一 · 从第一天就按两个用户写

期 8 的餐厅要复用这套。第一个用户很容易长满专属假设（"客人就是居民"
"价格就是 value"），到期 8 再抽象等于重写。`ShopKind` 那四个函数
从一开始就要留着。

### 二 · 结算必须确定性

同一天同一份货架算出来必须一样。用 `hashSeed(instanceId|worldDayId)`，
不用 `Math.random()`——否则玩家发现"今天没卖出去"就重开游戏重摇。
和每日任务抽签、水獭想要清单同一条判据。

### 三 · 离线补算的封顶靠货架，不靠天数上限

加天数上限 = 惩罚离线。货架空了自然停，这是动森的"产出补但封顶"，
而且我们连规则都不用写。

### 四 · 摆出去的家具不能直接卖

上架的是**背包里的**。屋里摆着的要先收回背包才能上架——否则玩家会
误把正在用的桌子卖掉，而且 `ownedCountFn`（开箱的"已经有了"判据）
会跟着乱。

---

## 验收

### 命令

```bash
cd Core && npm test && npm run typecheck:tests
cd ../Backend && npm test && npm run typecheck:tests
cd ../Frontend-3D && npm test && npm run typecheck:tests && npx tsc -b && npm run build
```

### 新增用例

| # | 用例 | 判据 |
|---|---|---|
| 1 | 上架后翻一天，钱进库、货架清位 | `getGold()` 增加，槽位变 null |
| 2 | 报告有名字 | 昨日事实里有 `shop_sold` + itemId |
| 3 | 空货架翻十天零收入 | 金币不变 |
| 4 | 离线十天但只够卖三天的货 | 只结三天，第四天起 break |
| 5 | 结算确定性 | 同一天同货架算两次结果一致 |
| 6 | 预算读 economy 表 | 改表里的数，卖出量跟着变 |
| 7 | 贵的先被买走 | 摆一件 90 一件 20，预算 100 时买走 90 那件 |
| 8 | 只收家具 | 往货架塞食材被拒 |
| 9 | 客源是函数 | 没有居民时零成交（不崩） |
| 10 | 升级换货位 | l2 的槽位比 l1 多 |

### 浏览器回路

| 步骤 | 期望 |
|---|---|
| 三位住齐 → 拿到图纸 → 盖起小店 | 石傀儡施工，完工能走进去 |
| 走进去按 F | 上架面板：左背包右货架，每件标着值多少 |
| 摆三件家具，`/day` | 金币进库，货架少了几件，`/facts` 里有"谁买走了什么" |
| 空着货架 `/day` 十次 | 一分钱没有，不报错 |
| 摆满后关游戏、把系统时钟拨后五天、重开 | 按实结算到货卖完为止 |

---

## 刻意不做的

- **玩家定价 / 讨价还价**（Recettear 那一路）
- **客人到店的演出**（他们"来过"是结算的结果，不是一段动画；
  真要演是独立一笔）
- **小镇散客**（客源函数已经留口，等小镇解锁那期）
- **店铺的招牌自定义**
- **餐厅**（期 8 复用这套骨架）

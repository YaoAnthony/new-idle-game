# BUG-15-05 · v46 迁移对只带一半形状的档直接崩（13 里发现并修）

- 严重度：P0（读档路径抛异常）
- 来源：13 全量用例：affection / visits 里用最小档跑 `migrateSave` → `Cannot read properties of undefined (reading 'unlockedFeatureIds')`
- 根因：迁移直接读 `save.ownWorld.progression`，没判空；老迁移都判过
- 修法：没有 progression 直接返回；`events ??= {}`；`migrations.test` 加 v46 两条
- 同类：以后迁移一律先判 `ownWorld?.progression`

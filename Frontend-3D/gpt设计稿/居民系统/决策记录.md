# 居民系统 · 决策记录

用户拍板过的事，按时间追加。任务文档引用这里，不重复解释。

| # | 决策 | 出处 |
|---|---|---|
| 1 | 「一只活物」这一层的统称从 **Pet 改成 Resident**。`CreatureRole.Pet` 作为四种身份之一保留 | 用户 2026-09-05 |
| 2 | 改名单独成任务、单独提交，零行为变化；存档字段 `WorldSave.pets`、文案键 `pet.*` 不动 | 00 任务 |
| 2b | 实例 id `pet-*` **改成 `resident-*`**，连存档一起迁（第二个提交） | 用户 2026-09-05 |
| 3 | `/pet` 调试指令改成 `/spawn`；居民相关的前端系统收进 `Frontend-3D/src/Game/Systems/residents/`（搬入 → `moveIn.ts`，指令 → `commands.ts`） | 用户 2026-09-05 |
| 4 | 行为按**角色挂载**：`ResidentAgent` 只留身体（走路、碰撞、寻路、存档），每种身份一个"脑子"文件（居民 / 工人 / 商人 / 宠物），各自决定下一步干什么。那个 1268 行的文件必须拆 | 用户 2026-09-05 |
| 5 | 居民房**夜里窗亮是纯表现**：由运行时"他在不在家"算出来，不进存档、不进联机状态 | 用户 2026-09-05 |
| 6 | **行动架构三层**：`Resident` 一个基类零子类（移动、说话、需求、存档）；**固定动词表**（Actions：`walk_to` `stand` `sit` `sleep` `hide` `show` `gesture` `work_at` `speak`，每个词 = 状态 + 时长 + 动画钩子，动画以后再换）；**技能注册表**（Skills：交易、建筑、作息、挖洞……各自 `decide` 出一个 Intent，只能用动词表里的词）。身份 = 技能组合，`CreatureRole` 只留作标签 | 用户 2026-09-05 |
| 7 | 动词必须能被指令直接驱动：`/npc <谁> do <动词> [参数]`，例 `/npc slime do walk_to 20 20`、`/npc slime do sit`。技能也要能开关：`/npc <谁> skill <id> on\|off` | 用户 2026-09-05 |
| 8 | **指令下达的动词立即生效**，打断当前一切（调试口必须立刻看到结果） | 用户 2026-09-05 |
| 9 | **技能之间允许打断**：更高优先级的技能算出 Intent 就抢过来（坐着坐着饿了起身去吃），不必等闲下来 | 用户 2026-09-05 |
| 10 | 三步三提交：00 改名 → 01 行动架构（零行为变化） → 02 写 routine。**先只写文档，不动代码** | 用户 2026-09-05 |
| 11 | 分多个任务执行，每个任务一份文档，写作标准同《小动物经济圈》：任何人不看聊天记录也能照着做完并验收 | 用户 2026-09-05 |
| 12 | **一个基类 `Resident`，每种小动物一个子类文件**，子类只声明挂哪些注册技能 + 覆盖少数钩子。推翻 `petAgent.ts` 文件头"实例化不继承"那条 | 用户 2026-09-05 |
| 13 | `walk_to` 的寻路要吃**地面代价**：以后铺了小路居民更愿意走路上。01 打通 `findRoute` 的 `costOf` 参数、默认全 1；铺路那期只填地表数据 | 用户 2026-09-05 |
| 14 | **联机：房主权威，房客是木偶。** 意图复制（`resident_intent` op）+ 2 Hz 关键帧纠偏（`sync:residents`）+ `pets` 刷新切片对账。房客不跑技能、不掷骰子、不发剧情信号；房客的 `/npc do` 与送礼被拒。**每个任务的验收都带联机双端截图**（用户："我可不想一个小动物 A 看在北边，B 看在南边"） | 用户要求 2026-09-05 · 模型为我按现成方案选的，用户可否 |
| 15 | Backend 的 `OP_KINDS` 手抄白名单搬进 Core（`WORLD_OP_KINDS` + 编译期断言），和 `WORLD_REFRESH_KEYS` 同一招。随 01 一起做 | 自查 2026-09-05 |
| 16 | 几何常数不进 gameplay 代码：门口偏移进 `BuildingLevel.doorstep`，访客入口进 `MapDefinition.visitorEntry` | 自查 2026-09-05 |
| 17 | 后果只走「信号 → storyRules → 效果」：好感加分（`adjust_affection`）、记忆（`add_memory`）、门口摆放（`porch_place`）、寄信（`send_letter`）、提出委托（`offer_favor`）、旗子（`set_flag`）全是剧情效果；任何系统文件里不得直接改这些 | 自查 2026-09-05，贯穿 03~13 |
| 18 | `speak` / `gesture` 是**并行槽**动词，不进串行队列；对玩家说的台词不同步给房客，居民之间说的同步 | 03 / 06 |
| 19 | 房客与房主邻居**不建立好感**、不能送礼 / 委托 / 邀请 / 收信附件；能闲聊、能进居民房、能看信箱 | 04 / 05 / 08 / 09 / 10（对齐动森做客规则） |
| 20 | 不做：搬走、吵架、减分、评分面板、实时小游戏、服装、自动回信、自由输入 | 功能对照表第九节 |
| 21 | 新增刷新切片（按任务）：`pets`(01) `favors`(05) `porch`(07) `interiors`(08) `mailbox`(10) `flags`(11)；关键帧字段一次定全（`expression` `speaking` `heldProp`） | 01 / 各任务 |
| 22 | 所有抽签（闲聊段、委托、访客、赠礼、来信、活动）用 `hashSeed(种子 + worldDayId + 次数)`；保底用 `storyPools` 那套 base/step/max；`Math.random` 只许出现在动画抖动 | 贯穿 |
| 23 | **开场只有一张条子**，文案照用户原文三行、不加一字；不引导、不列任务、不发第二封信、不写独白，玩家自己研究。魔女不出场，以后主线来信都是这个懒散口吻 | 用户 2026-09-05 |
| 24 | 薇尔住**树洞**：院子开局就有一棵特别大的树，她的图纸是"树洞门"、只能用在那棵树上，小屋型号退役。详见附-薇尔的大树 | 用户 2026-09-05（图纸固定选址是我的落法） |
| 25 | **两轮修 bug**：15 在 07 之后、16 在 14 之后，只修不加。验收看"没坏别的 + 以后不再坏"：每个 bug 先红后绿、根因进提交；P0/P1 归零才退出；硬编码守卫进 CI；16 加 soak / 性能 / 内容审计 / 五人房压测 | 用户要求 2026-09-05，标准为我拟 |
| 26 | 居民房的室内是**同图内景**（房间就在院子里，07 之前已能走进去），不是店铺那样的另一张图：联机会话里不许切图，另一张图会让房客进不了居民房；回家 = 真的走进屋、门锁跟主人位置走。l2 大屋和三件专属陈设等参考图 | 08 自查 2026-09-06（模型为我按现成架构选的，用户可否） |
| 27 | 三位老居民的"抽签到来"规则（`resident_*_arrives`）退役，到来只走桥头访客一条路；`/npc join` 保留为调试口。领地空位不做对话条件，在候选过滤里用真的选址校验扫 | 09 自查 2026-09-06 |
| 28 | 触发器加通用门槛 `StoryTrigger.requires`（对话条件表直接当触发条件），以后不再给触发器加专用字段；通用旗子 `WorldSave.flags` 进切片 | 11 自查 2026-09-06 |
| 29 | 活动表接管"到了场所做什么"，`routine.visitIntent` 不再写 if；道具 = `Intent.prop`（随 Intent）+ `weatherProp`（随天气），都不进存档；挂点按包围盒算，不让配方声明 | 12 自查 2026-09-06 |
| 30 | 小镇由阿茜第二幕的委托解锁（出入口 `requiresFeature`），老档 v46 迁移补开；立幕规则一律卡前一幕（`requiresEventStage`），调试口和迟到的信号都推不倒线；薇尔第三幕不写占位 | 13 自查 2026-09-06 |
| 31 | 门上的条子 = 门定义的 `noteFlag` + 旗子的值（letterId），不加存档字段；开场只有 `opening_note` 一条规则、原文照抄 | 14 自查 2026-09-06 |
